const FetchGroupsJoined = () => {
  // fetch the groups here.

  //groupsJoined will be empty initially when the
  //user will join the group for the first time
  const groupsJoined: [] = [];

  // returning the dummy object
  return groupsJoined;
};

export default FetchGroupsJoined;
